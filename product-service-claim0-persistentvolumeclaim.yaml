apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: product-service-claim0
  name: product-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
